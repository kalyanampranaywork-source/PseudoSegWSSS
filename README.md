# PseudoSegWSSS
This project is a variation of weakly supervised semantic segmentation. Here in this project will use patch-level labels instead of pixel-level mask and then we train the model and predict mask and then we extract labels from that weak mask and train the model again with pseudo-sag labels and true patch-level labels.
